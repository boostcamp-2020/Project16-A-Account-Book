import React from 'react';
import * as S from './style';

const ChattingBox = (): React.ReactElement => {
  return (
    <S.ChattingBox>
      <S.ChattingArea />
    </S.ChattingBox>
  );
};

export default ChattingBox;
