import styled from 'styled-components';

export const Wrap = styled.div`
  .mainTitle {
    font-size: 5rem;
  }
  img:not(:first-child) {
    width: 350px;
  }

  position: absolute;
  top: 3rem;
`;

export default {};
